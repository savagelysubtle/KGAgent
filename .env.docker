# ============================================================================
# KG Agent - Docker Environment Configuration
# ============================================================================
# Copy this file to .env and customize values as needed
# ============================================================================

# =============================================================================
# Service Ports
# =============================================================================
API_PORT=8000
DASHBOARD_PORT=3000
REDIS_PORT=6379
FALKORDB_PORT=6380

# =============================================================================
# Application Settings
# =============================================================================
LOG_LEVEL=INFO

# =============================================================================
# Database Passwords
# =============================================================================
# FalkorDB password (change in production!)
FALKORDB_PASSWORD=kgagent123

# =============================================================================
# LLM Configuration
# =============================================================================
# Default: LM Studio running on host machine
# Change these if using a different LLM service (e.g., OpenAI, Ollama)

# For LM Studio (default - runs on host machine)
LLM_BASE_URL=http://host.docker.internal:1234/v1
LLM_API_KEY=lm-studio
LLM_MODEL_NAME=local-model

# For OpenAI (uncomment and set your key)
# LLM_BASE_URL=https://api.openai.com/v1
# LLM_API_KEY=sk-your-api-key-here
# LLM_MODEL_NAME=gpt-4o-mini

# For Ollama (if running on host)
# LLM_BASE_URL=http://host.docker.internal:11434/v1
# LLM_API_KEY=ollama
# LLM_MODEL_NAME=llama3.2

# =============================================================================
# Embedding Model
# =============================================================================
# HuggingFace model for text embeddings
# This will be downloaded automatically on first run
HF_EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2

# =============================================================================
# Advanced Settings (usually don't need to change)
# =============================================================================
# ChromaDB collection name
CHROMA_COLLECTION_NAME=document_chunks

# Graph database driver (falkordb is recommended)
GRAPH_DRIVER=falkordb

